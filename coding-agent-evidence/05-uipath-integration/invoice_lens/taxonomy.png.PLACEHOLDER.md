# PLACEHOLDER → replace with `taxonomy.png`

Delete this file once you've saved the real screenshot as `taxonomy.png` in this folder.

**What this screenshot must show:** the InvoiceLens Generative Extraction taxonomy open in
the UiPath Document Understanding portal, with the `demurrage_detention_invoice` document
type expanded so the field list is visible (invoice_number, container_number,
total_amount_usd, the dates, etc.). The goal is to prove the taxonomy from
`uipath/document_understanding/invoice_lens_taxonomy.json` was loaded into the project.
